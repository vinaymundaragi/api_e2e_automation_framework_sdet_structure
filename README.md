# api_e2e_automation_framework_sdet_structure
A layered API automation framework using Playwright with a focus on scalability, maintainability, and CI integration.
The framework separates API clients, test data, validation logic, fixtures, and environment configuration.
This ensures clean test code and easy extensibility when new APIs are added.

